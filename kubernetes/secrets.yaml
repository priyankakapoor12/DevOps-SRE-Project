apiVersion: v1
kind: Secret
metadata:
  name: task-manager-secrets
type: Opaque
stringData:
  # These values should be replaced with actual secrets in production
  # Use AWS Secrets Manager or External Secrets Operator for production
  database-url: "postgresql+psycopg://user:password@rds-endpoint:5432/taskdb"
